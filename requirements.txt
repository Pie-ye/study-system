# Core API (always required)
fastapi==0.139.2
uvicorn[standard]==0.51.0
httpx==0.28.1
pydantic==2.13.4
python-dotenv==1.2.2

# Optional memory-context (chromadb + fastembed). Heavy; keep versions pinned.
chromadb==1.5.9
fastembed==0.8.0
